# Namespaced feature key verification

## Contract and diagnosis

Baseline 250c0ca; execution source support-namespaced-feature-keys/tasks.md.
The requester authorized continuing the existing PR repair and isolated
verification. Actual official 0.155.0-alpha.9 and candidate 0.155.0 CLIs both
emit guardianv2.thread_context. The former grammar rejected that one row in
each, whereas all 135 rows from 0.153.4 were accepted.

## Red / Green

The public inventory collection regression first failed with
`Feature-list output contains a malformed row`. After sharing a bounded dotted
identifier grammar, collection/comparison retains both the flat parent and
namespaced child with independent effective states. Additional tests cover
multi-segment names, empty segments, invalid characters, duplicate rows,
invalid boolean/stage, acceptance traces and blocking unclassified drift.

## Verification

- Focused feature/policy: 21 passed.
- Complete parity: 102 passed.
- Actual official, candidate and old bound CLIs: all feature inventories pass
  through the production collector in fresh isolated/effective homes, with
  145, 142 and 135 rows respectively. No provider or live configuration used.
- Profile regression: 227 passed.
- Python 3.9 AST syntax, diff whitespace and 26 strict OpenSpec items passed.
- Independent Spec and Standards reviews: no findings. Spec independently
  reran four focused tests successfully.
- Clean source archive plus the reviewed runtime changes: packaged, installed
  over the previous PR package and exercised through installed modules. All
  22 feature/policy/preparation cases passed; status and final package/installed
  manifest checks passed with no bytecode residue.
- Exact implementation commit a899eed was archived, packaged and installed in
  a new temporary root. Status, all 22 installed-module feature/policy/preparation
  tests and package/installed manifests passed. The local test runner now
  selects this verified package with bytecode and self-update disabled.
- Native Git pushed a899eed to existing PR #1. The PR description includes the
  namespace repair, current verification and remaining runtime-pair limits.
  No release, provider request or live installation was performed.

## Separate compatibility finding

With empty temporary configurations, the feature-only policy still reports
seven unclassified differences: analytics_plan_history, guardian_ext,
personality, realtime_conversation, send_message_to_user_async, use_xaa and
worktrees. This is separate from syntax. The repair grants no classification
exceptions and makes no full Desktop parity or successful live-update claim.
Changing those classifications requires capability/behavior evidence beyond
accepting the CLI's valid names; track this independently in TASK_LEDGER.md.

A further offline schema diagnostic using current adapter coverage and the
current acceptance trace found six protocol-policy errors: incompatible
thread/queue/add, thread/queue/update, thread/resume, turn/start and turn/steer,
plus unclassified internal-only thread/rollback. Both schemas generated and
loaded successfully. These are separate version-compatibility findings; no
schema exception or adapter change is included in this grammar repair.

## Review contracts

Two read-only reviews compare baseline 250c0ca to this working-tree repair.
Spec owns conformance to support-namespaced-feature-keys; Standards owns
engineering policy and code quality. Both may inspect source/tests/docs and
run bounded public-seam tests with bytecode disabled. No source/workflow writes,
live installation, provider calls or compatibility classification changes.
The primary agent owns fixes, integrated validation, commits and delivery.

Logs owned by this run use
`.planning/devflow/verification/local/namespaced-feature-*.log`.
